# Demo config for cms.
# Used by demo-setup.sh via CMS_CONFIG_DIR.
# search_paths is rewritten at setup time to point at the generated repos.

[general]
search_paths = [
  { path = "/tmp/cms-demo/repos", max_depth = 2 },
]

[finder]
include = ["sessions", "worktrees"]
